{
  "name": "jssec-demo",
  "version": "0.0.1",
  "description": "Demo of inserting a keylogger into HTTP requests through a comprimised DNS server",
  "scripts": {
    "start": "coffee app.coffee",
    "startauth": "authbind --deep coffee app.coffee"
  },
  "dependencies": {
    "coffee-script": "*",
    "http-proxy": "*",
    "native-dns": "*"
  }
}
